// A match over an enum without a wildcard must cover every variant
// EXPECT-ERROR: non-exhaustive match
enum Color {
    Rojo
    Verde
}

fn main() {
    let c = Color.Rojo;
    print(match c {
        Color.Rojo => 1,
    });
}
