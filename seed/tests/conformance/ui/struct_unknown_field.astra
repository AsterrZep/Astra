// Initialising a field the struct does not declare must be a compile error
// EXPECT-ERROR: has no field
struct Point {
    x: i32
    y: i32
}

fn main() {
    let p = Point { x: 1, y: 2, z: 3 };
    print(p.x);
}
