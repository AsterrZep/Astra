// Guards (`pattern if cond`) are specified in research/04 but are not part of
// Astra-0 yet; they must be rejected rather than silently ignored
// EXPECT-ERROR: guards are not supported
fn main() {
    let n = 3;
    print(match n {
        x if x > 0 => 1,
        _ => 2,
    });
}
