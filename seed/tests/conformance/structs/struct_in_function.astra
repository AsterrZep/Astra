// Structs as function parameters
// EXPECT: 30
// EXPECT: 10
struct Point {
    x: i32
    y: i32
}

fn sum(p: Point) -> i32 {
    return p.x + p.y;
}

fn main() {
    let p = Point { x: 10, y: 20 };
    print(sum(p));
    print(p.x);
}
