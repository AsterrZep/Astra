// Integer type annotations: i64, u32, u64 are recognized by the typechecker.
// These types are accepted in function signatures without "unknown type" errors.
// EXPECT: hello
fn uses_i64(x: i64) { }
fn uses_u32(x: u32) { }
fn uses_u64(x: u64) { }
fn main() {
    print("hello")
}
